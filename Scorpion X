-- =============================================================================
-- SCORPION X - UI LIBRARY ENGINE (CORRETTA E COMPLETA)
-- =============================================================================
local Library = {}

function Library:Inizializza(titoloHub)
    local Players = game:GetService("Players")
    local CoreGui = game:GetService("CoreGui")
    local TweenService = game:GetService("TweenService")
    local player = Players.LocalPlayer
    local playerGui = player:WaitForChild("PlayerGui", 10) or player.PlayerGui

    -- Rimuove vecchie istanze
    local oldGui = playerGui:FindFirstChild("ScorpioXMenu") or CoreGui:FindFirstChild("ScorpioXMenu")
    if oldGui then oldGui:Destroy() end

    local ScreenGui = Instance.new("ScreenGui")
    ScreenGui.Name = "ScorpioXMenu"
    ScreenGui.ResetOnSpawn = false
    ScreenGui.DisplayOrder = 100

    local successo = pcall(function() ScreenGui.Parent = CoreGui end)
    if not successo then ScreenGui.Parent = playerGui end

    -- Finestra Principale
    local MainFrame = Instance.new("Frame")
    MainFrame.Name = "MainFrame"
    MainFrame.Size = UDim2.new(0, 520, 0, 340)
    MainFrame.Position = UDim2.new(0.5, -260, 0.5, -170)
    MainFrame.BackgroundColor3 = Color3.fromRGB(15, 13, 20)
    MainFrame.Visible = true
    MainFrame.Active = true
    MainFrame.Draggable = true
    MainFrame.Parent = ScreenGui

    local UICornerFrame = Instance.new("UICorner")
    UICornerFrame.CornerRadius = UDim.new(0, 10)
    UICornerFrame.Parent = MainFrame

    local UIStrokeFrame = Instance.new("UIStroke")
    UIStrokeFrame.Thickness = 1.5
    UIStrokeFrame.Color = Color3.fromRGB(130, 50, 250)
    UIStrokeFrame.Parent = MainFrame

    -- Barra Superiore
    local TopBar = Instance.new("Frame")
    TopBar.Name = "TopBar"
    TopBar.Size = UDim2.new(1, 0, 0, 35)
    TopBar.BackgroundColor3 = Color3.fromRGB(22, 18, 30)
    TopBar.Parent = MainFrame

    local TopBarCorner = Instance.new("UICorner")
    TopBarCorner.CornerRadius = UDim.new(0, 10)
    TopBarCorner.Parent = TopBar

    local TitleLabel = Instance.new("TextLabel")
    TitleLabel.Size = UDim2.new(1, -20, 1, 0)
    TitleLabel.Position = UDim2.new(0, 15, 0, 0)
    TitleLabel.BackgroundTransparency = 1
    TitleLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
    TitleLabel.Font = Enum.Font.GothamBold
    TitleLabel.TextSize = 14
    TitleLabel.TextXAlignment = Enum.TextXAlignment.Left
    TitleLabel.Text = tostring(titoloHub):upper()
    TitleLabel.Parent = TopBar

    -- Pulsante Riapertura Menu
    local ToggleMenuBtn = Instance.new("TextButton")
    ToggleMenuBtn.Name = "ToggleMenuBtn"
    ToggleMenuBtn.Size = UDim2.new(0, 60, 0, 60)
    ToggleMenuBtn.Position = UDim2.new(0, 20, 0.3, 0)
    ToggleMenuBtn.BackgroundColor3 = Color3.fromRGB(130, 50, 250)
    ToggleMenuBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
    ToggleMenuBtn.Font = Enum.Font.GothamBold
    ToggleMenuBtn.TextSize = 12
    ToggleMenuBtn.Text = "SCO X"
    ToggleMenuBtn.Parent = ScreenGui

    local UICornerBtn = Instance.new("UICorner")
    UICornerBtn.CornerRadius = UDim.new(1, 0)
    UICornerBtn.Parent = ToggleMenuBtn

    ToggleMenuBtn.MouseButton1Click:Connect(function()
        MainFrame.Visible = not MainFrame.Visible
    end)

    -- Barra Laterale per i Bottoni delle Tab
    local SideBar = Instance.new("ScrollingFrame")
    SideBar.Name = "SideBar"
    SideBar.Size = UDim2.new(0, 130, 1, -45)
    SideBar.Position = UDim2.new(0, 10, 0, 40)
    SideBar.BackgroundTransparency = 1
    SideBar.CanvasSize = UDim2.new(0, 0, 0, 0)
    SideBar.AutomaticCanvasSize = Enum.AutomaticCanvasSize.Y
    SideBar.ScrollBarThickness = 0
    SideBar.Parent = MainFrame

    local SideBarList = Instance.new("UIListLayout")
    SideBarList.Padding = UDim.new(0, 5)
    SideBarList.SortOrder = Enum.SortOrder.LayoutOrder
    SideBarList.Parent = SideBar

    -- Contenitore delle Pagine
    local ContentContainer = Instance.new("Frame")
    ContentContainer.Name = "ContentContainer"
    ContentContainer.Size = UDim2.new(1, -160, 1, -45)
    ContentContainer.Position = UDim2.new(0, 150, 0, 40)
    ContentContainer.BackgroundTransparency = 1
    ContentContainer.Parent = MainFrame

    local InterfacciaEngine = { tabs = {}, tabButtons = {}, primaTab = nil }

    function InterfacciaEngine:CreaTab(name, order)
        local contentFrame = Instance.new("ScrollingFrame")
        contentFrame.Name = name .. "Tab"
        contentFrame.Size = UDim2.new(1, 0, 1, 0)
        contentFrame.BackgroundTransparency = 1
        contentFrame.CanvasSize = UDim2.new(0, 0, 0, 0)
        contentFrame.AutomaticCanvasSize = Enum.AutomaticCanvasSize.Y
        contentFrame.ScrollBarThickness = 3
        contentFrame.ScrollBarImageColor3 = Color3.fromRGB(130, 50, 250)
        contentFrame.Visible = false
        contentFrame.Parent = ContentContainer

        local listLayout = Instance.new("UIListLayout")
        listLayout.Padding = UDim.new(0, 6)
        listLayout.HorizontalAlignment = Enum.HorizontalAlignment.Center
        listLayout.Parent = contentFrame

        self.tabs[name] = contentFrame

        local tabBtn = Instance.new("TextButton")
        tabBtn.Size = UDim2.new(1, 0, 0, 32)
        tabBtn.BackgroundColor3 = Color3.fromRGB(22, 18, 30)
        tabBtn.TextColor3 = Color3.fromRGB(180, 180, 200)
        tabBtn.Font = Enum.Font.GothamSemibold
        tabBtn.TextSize = 12
        tabBtn.Text = " " .. name
        tabBtn.TextXAlignment = Enum.TextXAlignment.Left
        tabBtn.LayoutOrder = order
        tabBtn.Parent = SideBar

        local btnCorner = Instance.new("UICorner")
        btnCorner.CornerRadius = UDim.new(0, 6)
        btnCorner.Parent = tabBtn
        
        local btnStroke = Instance.new("UIStroke")
        btnStroke.Thickness = 1
        btnStroke.Color = Color3.fromRGB(35, 30, 50)
        btnStroke.Parent = tabBtn

        if not self.primaTab then
            self.primaTab = name
        end

        tabBtn.MouseButton1Click:Connect(function()
            for tName, frame in pairs(self.tabs) do 
                frame.Visible = (tName == name) 
            end
            for bName, btn in pairs(self.tabButtons) do
                if bName == name then
                    btn.BackgroundColor3 = Color3.fromRGB(130, 50, 250)
                    btn.TextColor3 = Color3.fromRGB(255, 255, 255)
                    btn.UIStroke.Color = Color3.fromRGB(160, 90, 255)
                else
                    btn.BackgroundColor3 = Color3.fromRGB(22, 18, 30)
                    btn.TextColor3 = Color3.fromRGB(180, 180, 200)
                    btn.UIStroke.Color = Color3.fromRGB(35, 30, 50)
                end
            end
        end)

        self.tabButtons[name] = tabBtn
        return contentFrame
    end

    function InterfacciaEngine:AggiungiToggle(parentTab, titleText, descText, defaultState, callback)
        local container = Instance.new("Frame")
        container.Size = UDim2.new(0.96, 0, 0, 45)
        container.BackgroundColor3 = Color3.fromRGB(22, 18, 30)
        container.Parent = parentTab

        local corner = Instance.new("UICorner")
        corner.CornerRadius = UDim.new(0, 6)
        corner.Parent = container
        
        local stroke = Instance.new("UIStroke")
        stroke.Thickness = 1
        stroke.Color = Color3.fromRGB(35, 30, 50)
        stroke.Parent = container

        local titleLabel = Instance.new("TextLabel")
        titleLabel.Size = UDim2.new(0.7, 0, 0, 20)
        titleLabel.Position = UDim2.new(0.03, 0, 0, 4)
        titleLabel.BackgroundTransparency = 1
        titleLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
        titleLabel.Font = Enum.Font.GothamMedium
        titleLabel.TextSize = 12
        titleLabel.TextXAlignment = Enum.TextXAlignment.Left
        titleLabel.Text = titleText
        titleLabel.Parent = container

        local descLabel = Instance.new("TextLabel")
        descLabel.Size = UDim2.new(0.7, 0, 0, 18)
        descLabel.Position = UDim2.new(0.03, 0, 0, 22)
        descLabel.BackgroundTransparency = 1
        descLabel.TextColor3 = Color3.fromRGB(140, 135, 150)
        descLabel.Font = Enum.Font.Gotham
        descLabel.TextSize = 9
        descLabel.TextXAlignment = Enum.TextXAlignment.Left
        descLabel.Text = descText
        descLabel.Parent = container

        local toggleBg = Instance.new("Frame")
        toggleBg.Size = UDim2.new(0, 38, 0, 20)
        toggleBg.Position = UDim2.new(0.95, -40, 0.5, -10)
        toggleBg.BackgroundColor3 = Color3.fromRGB(35, 30, 45)
        toggleBg.Parent = container

        local toggleBgCorner = Instance.new("UICorner")
        toggleBgCorner.CornerRadius = UDim.new(1, 0)
        toggleBgCorner.Parent = toggleBg

        local toggleBall = Instance.new("Frame")
        toggleBall.Size = UDim2.new(0, 14, 0, 14)
        toggleBall.Position = UDim2.new(0, 3, 0, 3)
        toggleBall.BackgroundColor3 = Color3.fromRGB(150, 150, 150)
        toggleBall.Parent = toggleBg

        local toggleBallCorner = Instance.new("UICorner")
        toggleBallCorner.CornerRadius = UDim.new(1, 0)
        toggleBallCorner.Parent = toggleBall

        local clickBtn = Instance.new("TextButton")
        clickBtn.Size = UDim2.new(1, 0, 1, 0)
        clickBtn.BackgroundTransparency = 1
        clickBtn.Text = ""
        clickBtn.Parent = container

        local state = defaultState
        local function updateVisual()
            toggleBg.BackgroundColor3 = state and Color3.fromRGB(130, 50, 250) or Color3.fromRGB(35, 30, 45)
            toggleBall.Position = state and UDim2.new(0, 21, 0, 3) or UDim2.new(0, 3, 0, 3)
            toggleBall.BackgroundColor3 = state and Color3.fromRGB(255, 255, 255) or Color3.fromRGB(150, 150, 150)
        end

        updateVisual()
        clickBtn.MouseButton1Click:Connect(function()
            state = not state
            updateVisual()
            callback(state)
        end)

        return container
    end

    function InterfacciaEngine:AggiungiDropdown(parentTab, options, defaultText, callback)
        local dropContainer = Instance.new("Frame")
        dropContainer.Size = UDim2.new(0.96, 0, 0, 32)
        dropContainer.BackgroundTransparency = 1
        dropContainer.Parent = parentTab

        local dropBtn = Instance.new("TextButton")
        dropBtn.Size = UDim2.new(1, 0, 1, 0)
        dropBtn.BackgroundColor3 = Color3.fromRGB(22, 18, 30)
        dropBtn.TextColor3 = Color3.fromRGB(200, 160, 255)
        dropBtn.Font = Enum.Font.GothamBold
        dropBtn.TextSize = 11
        dropBtn.Text = defaultText .. "  ▼"
        dropBtn.Parent = dropContainer

        local dropCorner = Instance.new("UICorner")
        dropCorner.CornerRadius = UDim.new(0, 6)
        dropCorner.Parent = dropBtn

        local dropListFrame = Instance.new("Frame")
        dropListFrame.Size = UDim2.new(1, 0, 0, 0)
        dropListFrame.Position = UDim2.new(0, 0, 1, 3)
        dropListFrame.BackgroundColor3 = Color3.fromRGB(18, 14, 24)
        dropListFrame.Visible = false
        dropListFrame.ZIndex = 50
        dropListFrame.Parent = dropContainer

        local dropLayout = Instance.new("UIListLayout")
        dropLayout.Parent = dropListFrame

        for _, val in ipairs(options) do
            local opt = Instance.new("TextButton")
            opt.Size = UDim2.new(1, 0, 0, 28)
            opt.BackgroundColor3 = Color3.fromRGB(25, 20, 35)
            opt.TextColor3 = Color3.fromRGB(255, 255, 255)
            opt.Font = Enum.Font.Gotham
            opt.TextSize = 11
            opt.Text = tostring(val.Text or val)
            opt.ZIndex = 51
            opt.Parent = dropListFrame

            opt.Activated:Connect(function()
                dropBtn.Text = tostring(val.Text or val) .. "  ▼"
                dropListFrame.Visible = false
                dropContainer.Size = UDim2.new(0.96, 0, 0, 32)
                callback(val.Value or val)
            end)
        end

        dropBtn.Activated:Connect(function()
            dropListFrame.Visible = not dropListFrame.Visible
            if dropListFrame.Visible then
                dropListFrame.Size = UDim2.new(1, 0, 0, #options * 28)
                dropContainer.Size = UDim2.new(0.96, 0, 0, 32 + (#options * 28) + 3)
            else
                dropContainer.Size = UDim2.new(0.96, 0, 0, 32)
            end
        end)
    end

    -- Attiva e mostra in automatico la prima scheda generata
    task.spawn(function()
        task.wait(0.3)
        if self.primaTab then
            self.tabs[self.primaTab].Visible = true
            local btn = self.tabButtons[self.primaTab]
            if btn then
                btn.BackgroundColor3 = Color3.fromRGB(130, 50, 250)
                btn.TextColor3 = Color3.fromRGB(255, 255, 255)
                btn.UIStroke.Color = Color3.fromRGB(160, 90, 255)
            end
        end
    end)

    return InterfacciaEngine
end

return Library
